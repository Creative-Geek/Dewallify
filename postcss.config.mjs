// This file configures PostCSS, specifying the plugins to be used in the CSS build process. It is essential for processing Tailwind CSS.

const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;